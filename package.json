{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "release": "lerna publish --no-private"
  },
  "devDependencies": {
    "lerna": "^6.6.1",
    "turbo": "^1.9.3"
  }
}
