{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "publish-packages": "lerna publish --no-private"
  },
  "devDependencies": {
    "lerna": "^6.6.2",
    "turbo": "^1.10.6"
  }
}
